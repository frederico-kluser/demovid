{
  "routing": {
    "must_trigger": [
      "add a new action verb to the storyboard schema",
      "the model returned a storyboard with the wrong shape and zod rejected it",
      "add a locale overlay for a new language preset",
      "tune the dwell time and pacing of a preset"
    ],
    "must_not_trigger": [
      "the overlay scaled with the page during zoom",
      "the recorder left a process running"
    ]
  },
  "claims": [
    {
      "assert": "code-contains",
      "path": "src/storyboard.ts",
      "needle": "superRefine",
      "why": "the zod layer that carries every cross-field rule"
    },
    {
      "assert": "code-lacks",
      "path": "src/storyboard.ts",
      "needle": "minLength:",
      "why": "the JSON-Schema KEY form. The bare word also appears in the comment listing what strict:true forbids, so a broader needle fails on its own documentation"
    },
    {
      "assert": "code-contains",
      "path": "src/storyboard.ts",
      "needle": "STORYBOARD_JSON_SCHEMA",
      "why": "the hand-written schema the system prompt carries"
    },
    {
      "assert": "code-contains",
      "path": "src/openai/responses.ts",
      "needle": "response_format",
      "why": "the only shape lever DeepSeek offers. The whole skill turns on this being json_object rather than json_schema+strict — if a migration back to server-side enforcement lands, this claim is where the skill should be re-read. Comments are stripped before matching, so this hits the real call body, not the header prose that also names it"
    },
    {
      "assert": "code-lacks",
      "path": "src/openai/responses.ts",
      "needle": "previous_response_id",
      "why": "Chat Completions is stateless, which is WHY every repair has to re-send the inventory. The migration left this word in the header comment explaining its own removal, and comment-stripping is what keeps that from satisfying a claim about behaviour — the exact failure the stripper was built for"
    },
    {
      "assert": "code-contains",
      "path": "src/presets/index.ts",
      "needle": "export function applyLocale",
      "why": "locale is a separate composable layer, not another preset"
    },
    {
      "assert": "code-contains",
      "path": "src/presets/index.ts",
      "needle": "export function dwellFor",
      "why": "the three-input dwell calculation the skill documents"
    },
    {
      "assert": "code-contains",
      "path": "src/presets/locale/pt-BR.ts",
      "needle": "requireHumanListen",
      "why": "the honest record of an unverified accent claim"
    },
    {
      "assert": "npm-script",
      "name": "test",
      "why": "declared verification signal"
    }
  ]
}

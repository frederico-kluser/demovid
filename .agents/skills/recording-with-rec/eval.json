{
  "routing": {
    "must_trigger": [
      "the recorder child process stays alive after the script exits",
      "how do I pause and resume the screen recorder",
      "the mp4 came out truncated and will not play",
      "resolve the browser window id for the capture",
      "the browser window opened partly off the monitor during recording"
    ],
    "must_not_trigger": [
      "magnified text looks blurry during the zoom",
      "split narration into sentences before calling the api"
    ]
  },
  "claims": [
    {
      "assert": "code-contains",
      "path": "src/recorder/index.ts",
      "needle": "SIGUSR2",
      "why": "the SIGNAL ACTUALLY SENT, not the comment describing it. file-contains stayed green through a simulated refactor because the token survived in prose"
    },
    {
      "assert": "code-contains",
      "path": "src/recorder/index.ts",
      "needle": "signalCode === null",
      "why": "the liveness predicate that replaced the lying child.killed"
    },
    {
      "assert": "code-lacks",
      "path": "src/recorder/index.ts",
      "needle": "!this.#child.killed",
      "why": "the USAGE form. The bare token also appears in the comment explaining why not to use it, so a needle that broad is a false-positive generator"
    },
    {
      "assert": "code-contains",
      "path": "src/recorder/index.ts",
      "needle": "async dispose()",
      "why": "unconditional cleanup is what prevents orphaned recorders"
    },
    {
      "assert": "code-contains",
      "path": "src/browser.ts",
      "needle": "reducedMotion",
      "why": "without no-preference, WAAPI flattens every camera move"
    },
    {
      "assert": "code-contains",
      "path": "src/browser.ts",
      "needle": "findNewWindow",
      "why": "positive form: the window is found by diffing the X11 set. Asserting the ABSENCE of \"search --name\" fails on the comment that forbids it"
    },
    {
      "assert": "npm-script",
      "name": "test:record",
      "why": "declared verification signal"
    },
    {
      "assert": "code-contains",
      "path": "src/recorder/backend-ffmpeg.ts",
      "needle": "crop=trunc(iw/2)*2:trunc(ih/2)*2",
      "why": "libx264 recusa dimensão ímpar e deixa MP4 sem moov atom — janela de browser é rotineiramente ímpar"
    },
    {
      "assert": "code-contains",
      "path": "src/recorder/session-env.ts",
      "needle": "lastIndexOf(\")\")",
      "why": "o campo 2 de /proc/<pid>/stat é o nome do processo entre parênteses e pode conter parênteses; split ingênuo desalinha todos os campos seguintes"
    },
    {
      "assert": "code-contains",
      "path": "src/record.ts",
      "needle": "plan.usable",
      "why": "the clamp for the browser own UI. plan.window is the CONTENT and the OS window is content plus tab strip, so without this the window overflows the work area even when the content fits"
    },
    {
      "assert": "code-contains",
      "path": "src/resolution.ts",
      "needle": "usable",
      "why": "planCapture must publish the box it fitted against, because the only place the chrome height is knowable is after launch"
    },
    {
      "assert": "code-contains",
      "path": "src/record.ts",
      "needle": "kind: \"window\"",
      "why": "captura por região grava o que estiver na frente — uma tomada de teste saiu com as conversas privadas do operador"
    }
  ]
}
